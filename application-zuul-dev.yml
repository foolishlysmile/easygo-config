server:
  port: 8023
spring:
  application:
    name: ZUUL-GATEWAY
zuul:
  routes:
    myPlat.serviceId: EASYGO-PLAT
    myPlat.path: /plat/**
  routes:
    myProduct.serviceId: EASYGO-PRODUCT
    myProduct.path: /product/**
  ignored-services: "*"
  prefix: "/services"